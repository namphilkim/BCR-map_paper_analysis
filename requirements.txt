# PyPI dependencies for BCR-map (see pyproject.toml for metadata). For a frozen supplement,
# run: pip freeze > requirements-lock.txt after validating on your target hardware.
# Example stack: torch 1.13.x, pytorch-lightning 2.0.x, transformers 4.44.x
typer>=0.9.0
torch>=1.13,<3
pytorch-lightning>=2.0,<3
transformers>=4.30
torchmetrics>=1.0
torchvision
pandas
numpy
Pillow
h5py
tqdm
PyYAML